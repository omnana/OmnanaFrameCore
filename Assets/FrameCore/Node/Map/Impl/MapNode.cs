namespace FrameCore.Runtime
{
    public sealed class MapNode : BaseNode
    {
        protected override void AddController(IController controller)
        {
        }

        protected override void RemoveController(IController controller)
        {
        }
    }
}